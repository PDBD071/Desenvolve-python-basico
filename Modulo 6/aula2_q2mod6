import random

# Gere um valor aleatório entre 5 e 20 e armazene em num_elementos
num_elementos = random.randint(5, 20)

# Gere uma lista com num_elementos valores aleatórios entre 1 e 10
elementos = [random.randint(1, 10) for _ in range(num_elementos)]

# Calcule a soma e a média dos valores da lista
soma = sum(elementos)
media = soma / num_elementos

# Imprima a lista, a soma e a média
print("Lista elementos:", elementos)
print("Soma dos valores:", soma)
print("Média dos valores:", media)
